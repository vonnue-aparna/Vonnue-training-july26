function updateUser(users, id, changes) {

    for (let index = 0; users < array.length; index++) {
        
        if(users[i].id === id) {
            
        }
        
    }
}

const users = [
    { 
        id: 1, 
        name: "Asha", 
        profile: { city: "Pune", active: true } 
    }
];

updateUser(users, 1, {name: "Asha Khan"});
