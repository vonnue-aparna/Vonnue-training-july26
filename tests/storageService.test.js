import {describe, it, expect} from "vitest";
import { saveStorage } from "../js/storageService.js"

describe("Task Manager", ()=> {
    it("checks whether data is loaded", ()=> {
        TEST_KEY = "test-key";

        
    })
});

