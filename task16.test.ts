// i think this can be tested by spying on the global Storage and the getItem method by mocking the implementation.
// if that is the correct implementation i currently can't recall the syntax for it

import { describe, test } from "vitest";

describe("testing task 16", () => {
  test("--- testing localStorage retrivel", () => {});
});
